
describe('PUT users request', () => {
  test('it should update the user with the correct fields', () => {

    const userData = { name: 'Charlie', email: 'charlie@email.com', password: 'charlie123', confirmPassword: 'charlie123' }

    expect(userData.body);
  });
});